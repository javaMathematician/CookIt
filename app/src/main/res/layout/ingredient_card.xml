<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ingredientCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="1dp"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="@color/darkThemeBackgroundCardColor"
    app:cardCornerRadius="0dp"
    app:cardElevation="4dp"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent">

    <LinearLayout
        android:id="@+id/linearLayoutOnIngredient"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/regular_card"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/imageOnIngredient"
            android:layout_width="130dp"
            android:layout_height="130dp"
            android:layout_gravity="center"
            android:layout_margin="6dp"
            android:src="@android:color/white" />

        <TextView
            android:id="@+id/textOnIngredient"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:gravity="center_vertical"
            android:textColor="@color/darkThemeTextColor"
            android:textSize="18sp" />

        <CheckBox
            android:id="@+id/checkBoxOnIngredient"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight=".5"
            android:clickable="false"
            android:focusable="false"
            android:gravity="center"
            android:theme="@style/checkBoxStyle" />
    </LinearLayout>
</androidx.cardview.widget.CardView>